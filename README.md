htmllint.github.io
==================

Getting Started
---------------

Make sure you have the `jekyll` gem installed:

```
$ gem install jekyll
```

Then run the following to preview the site locally:

```
$ jekyll serve --watch
```
